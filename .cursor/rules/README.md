# Rules (global)

Repo-local rule copies were removed. Edit `~/.cursor/rules/architecture.mdc` (alwaysApply).

Regenerate [`CLAUDE.md`](../../CLAUDE.md) with `./.claude/generate-rules.sh` after rule changes.
