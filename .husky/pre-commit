#!/usr/bin/env sh

set -e

echo "Running TypeScript check (pnpm typecheck)..."
pnpm typecheck

echo "Running Checkstyle (Google)..."
./gradlew checkstyleMain checkstyleTest --no-daemon

echo "Pre-commit checks passed!"
